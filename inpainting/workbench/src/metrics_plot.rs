use crate::MethodMetrics;
//use plotters::prelude::*;

pub fn plot_metrics(metrics: &MethodMetrics, name: &str) {
    // let root = BitMapBackend::new(name, (640, 480)).into_drawing_area()
    // ;
}
